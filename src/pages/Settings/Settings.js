import React, {useState, useEffect} from 'react';
import API from '../../components/utils/API';
import './Settings.css'

function Settings({token, isLoggedIn}) {

  return (
    <div className='settingsContainer'>
        <h1>Settings</h1>
    </div>
  );
}

export default Settings;